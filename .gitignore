node_modules
dist
.claude/*
!.claude/agents/
!.claude/commands/
!.claude/hooks/
!.claude/settings.json
.claude/settings.local.json
.env
.env.local
.env*.local
.env.production
*.log
*.tsbuildinfo
.vercel
coverage

playwright-report
playwright/.auth
test-results

# One-shot seed tool output (scripts/seed-spots.py) — large data, regenerated
scripts/output/

# Large media files in src/ (use public/ for static assets)
src/**/*.mp4
src/**/*.webm
src/**/*.mov

# Capacitor native platforms — both tracked so we can customise manifests,
# entitlements, icons, signing configs, and Info.plist. Only build artefacts
# and per-developer Xcode state are ignored below.

# iOS (Capacitor/Xcode) — tracked project, ignore build + pod artefacts
ios/App/Pods/
ios/App/*.xcworkspace/xcuserdata/
ios/App/App.xcodeproj/xcuserdata/
ios/App/App.xcodeproj/project.xcworkspace/xcuserdata/
ios/App/build/
ios/App/DerivedData/
ios/build/
# Firebase config — secret, never commit. Downloaded per ios/NATIVE_SETUP.md.
ios/App/App/GoogleService-Info.plist
android/app/google-services.json
# Podfile.lock: intentionally NOT listed here, but the repo has no Podfile
# either. The Xcode project (App.xcodeproj) resolves native plugins via Swift
# Package Manager (ios/App/CapApp-SPM/Package.swift), not CocoaPods — so no
# Podfile.lock is produced by the current setup. The CocoaPods flow described
# in ios/README.md / ios/NATIVE_SETUP.md contradicts this and is UNVERIFIED;
# which native path actually builds must be confirmed with Xcode on a Mac
# before deciding what (if anything) to commit here. Do not add a Podfile.lock
# ignore until that decision is made.
